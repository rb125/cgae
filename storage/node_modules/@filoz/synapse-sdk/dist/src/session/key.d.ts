import * as SK from '@filoz/synapse-core/session-key';
import { type Account, type Chain, type Client, type Hash, type Transport } from 'viem';
export declare class SessionKey {
    private readonly _ownerClient;
    private readonly _client;
    private readonly _account;
    private readonly _chain;
    constructor(client: Client<Transport, Chain, Account>, account: Account);
    get account(): Account;
    get chain(): Chain;
    get client(): Client<Transport, Chain, Account>;
    fetchExpiries(permissions?: SK.SessionKeyPermissions[]): Promise<Record<SK.SessionKeyPermissions, bigint>>;
    login(expiry: bigint, permissions?: SK.SessionKeyPermissions[], origin?: string): Promise<Hash>;
    revoke(permissions?: SK.SessionKeyPermissions[], origin?: string): Promise<Hash>;
}
//# sourceMappingURL=key.d.ts.map