import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getRail(client, options) {
    return readContract(client, getRailCall({
        chain: client.chain,
        railId: options.railId,
        contractAddress: options.contractAddress,
    }));
}
export function getRailCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.filecoinPay.abi,
        address: options.contractAddress ?? chain.contracts.filecoinPay.address,
        functionName: 'getRail',
        args: [options.railId],
    };
}
//# sourceMappingURL=get-rail.js.map