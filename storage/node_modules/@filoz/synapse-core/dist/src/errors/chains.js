import { calibration, devnet, mainnet } from "../chains.js";
import { isSynapseError, SynapseError } from "./base.js";
export class UnsupportedChainError extends SynapseError {
    name = 'UnsupportedChainError';
    constructor(chainId) {
        super(`Unsupported chain: ${chainId} (only Filecoin mainnet (${mainnet.id}), calibration (${calibration.id}), and devnet (${devnet.id}) are supported). Import chains from @filoz/synapse-core/chains to get the correct chain.`);
    }
    static is(value) {
        return isSynapseError(value) && value.name === 'UnsupportedChainError';
    }
}
//# sourceMappingURL=chains.js.map