import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getScheduledRemovals(client, options) {
    const data = await readContract(client, getScheduledRemovalsCall({
        chain: client.chain,
        dataSetId: options.dataSetId,
        contractAddress: options.contractAddress,
    }));
    return Array.from(new Set(data));
}
export function getScheduledRemovalsCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.pdp.abi,
        address: options.contractAddress ?? chain.contracts.pdp.address,
        functionName: 'getScheduledRemovals',
        args: [options.dataSetId],
    };
}
//# sourceMappingURL=get-scheduled-removals.js.map