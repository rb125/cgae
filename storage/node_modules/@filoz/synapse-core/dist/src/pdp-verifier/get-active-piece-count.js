import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getActivePieceCount(client, options) {
    const data = await readContract(client, getActivePieceCountCall({
        chain: client.chain,
        dataSetId: options.dataSetId,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function getActivePieceCountCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.pdp.abi,
        address: options.contractAddress ?? chain.contracts.pdp.address,
        functionName: 'getActivePieceCount',
        args: [options.dataSetId],
    };
}
//# sourceMappingURL=get-active-piece-count.js.map