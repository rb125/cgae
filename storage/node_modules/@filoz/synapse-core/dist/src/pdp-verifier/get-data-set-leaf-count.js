import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getDataSetLeafCount(client, options) {
    const data = await readContract(client, getDataSetLeafCountCall({
        chain: client.chain,
        dataSetId: options.dataSetId,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function getDataSetLeafCountCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.pdp.abi,
        address: options.contractAddress ?? chain.contracts.pdp.address,
        functionName: 'getDataSetLeafCount',
        args: [options.dataSetId],
    };
}
//# sourceMappingURL=get-data-set-leaf-count.js.map