import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getDataSetStorageProvider(client, options) {
    const [storageProvider, proposedStorageProvider] = await readContract(client, getDataSetStorageProviderCall({
        chain: client.chain,
        dataSetId: options.dataSetId,
        contractAddress: options.contractAddress,
    }));
    return [storageProvider, proposedStorageProvider];
}
export function getDataSetStorageProviderCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.pdp.abi,
        address: options.contractAddress ?? chain.contracts.pdp.address,
        functionName: 'getDataSetStorageProvider',
        args: [options.dataSetId],
    };
}
//# sourceMappingURL=get-data-set-storage-provider.js.map