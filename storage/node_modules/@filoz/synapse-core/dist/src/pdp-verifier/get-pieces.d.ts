import type { Simplify } from 'type-fest';
import type { Address, Chain, Client, ReadContractErrorType, Transport } from 'viem';
import { asChain } from '../chains.ts';
import type { PdpDataSet, Piece, PieceWithMetadata } from '../warm-storage/types.ts';
import { type getActivePieces } from './get-active-pieces.ts';
export declare namespace getPieces {
    type OptionsType = Simplify<Omit<getActivePieces.OptionsType, 'dataSetId'> & {
        dataSet: PdpDataSet;
        address: Address;
    }>;
    type OutputType = {
        pieces: Piece[];
        hasMore: boolean;
    };
    type ErrorType = asChain.ErrorType | ReadContractErrorType;
}
export declare function getPieces(client: Client<Transport, Chain>, options: getPieces.OptionsType): Promise<getPieces.OutputType>;
export declare namespace getPiecesWithMetadata {
    type OptionsType = Simplify<Omit<getActivePieces.OptionsType, 'dataSetId'> & {
        dataSet: PdpDataSet;
        address: Address;
    }>;
    type OutputType = {
        pieces: PieceWithMetadata[];
        hasMore: boolean;
    };
    type ErrorType = asChain.ErrorType | ReadContractErrorType;
}
export declare function getPiecesWithMetadata(client: Client<Transport, Chain>, options: getPiecesWithMetadata.OptionsType): Promise<getPiecesWithMetadata.OutputType>;
//# sourceMappingURL=get-pieces.d.ts.map