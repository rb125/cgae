import type { Account, Address, Chain, Client, Transport } from 'viem';
import { type SessionKeyPermissions } from './permissions.ts';
export type IsExpiredOptions = {
    address: Address;
    sessionKeyAddress: Address;
    permission: SessionKeyPermissions;
};
export declare function isExpired(client: Client<Transport, Chain>, options: IsExpiredOptions): Promise<boolean>;
export type LoginOptions = {
    address: Address;
    permissions: SessionKeyPermissions[];
    expiresAt?: bigint;
    origin?: string;
};
export declare function login(client: Client<Transport, Chain, Account>, options: LoginOptions): Promise<`0x${string}`>;
export type RevokeOptions = {
    address: Address;
    permissions: SessionKeyPermissions[];
    origin?: string;
};
export declare function revoke(client: Client<Transport, Chain, Account>, options: RevokeOptions): Promise<`0x${string}`>;
//# sourceMappingURL=actions.d.ts.map