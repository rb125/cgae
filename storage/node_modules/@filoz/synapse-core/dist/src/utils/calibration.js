import { request } from 'iso-web/http';
export async function claimTokens(options) {
    const response = await request.json.get(`https://forest-explorer.chainsafe.dev/api/claim_token_all?address=${options.address}`, {
        timeout: 20000,
    });
    if (response.error) {
        throw new Error(response.error.cause[0].error.ServerError);
    }
    return response.result;
}
//# sourceMappingURL=calibration.js.map