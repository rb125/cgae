import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function activeProviderCount(client, options = {}) {
    const data = await readContract(client, activeProviderCountCall({
        chain: client.chain,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function activeProviderCountCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.serviceProviderRegistry.abi,
        address: options.contractAddress ?? chain.contracts.serviceProviderRegistry.address,
        functionName: 'activeProviderCount',
        args: [],
    };
}
//# sourceMappingURL=active-provider-count.js.map