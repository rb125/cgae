import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getProvidersByProductType(client, options) {
    const data = await readContract(client, getProvidersByProductTypeCall({
        chain: client.chain,
        productType: options.productType,
        onlyActive: options.onlyActive,
        offset: options.offset,
        limit: options.limit,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function getProvidersByProductTypeCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.serviceProviderRegistry.abi,
        address: options.contractAddress ?? chain.contracts.serviceProviderRegistry.address,
        functionName: 'getProvidersByProductType',
        args: [options.productType, options.onlyActive ?? true, options.offset ?? 0n, options.limit ?? 50n],
    };
}
//# sourceMappingURL=get-providers-by-product-type.js.map