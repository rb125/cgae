import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getProviderWithProduct(client, options) {
    const data = await readContract(client, getProviderWithProductCall({
        chain: client.chain,
        providerId: options.providerId,
        productType: options.productType,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function getProviderWithProductCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.serviceProviderRegistry.abi,
        address: options.contractAddress ?? chain.contracts.serviceProviderRegistry.address,
        functionName: 'getProviderWithProduct',
        args: [options.providerId, options.productType],
    };
}
//# sourceMappingURL=get-provider-with-product.js.map