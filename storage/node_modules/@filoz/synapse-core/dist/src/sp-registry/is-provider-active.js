import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function isProviderActive(client, options) {
    const data = await readContract(client, isProviderActiveCall({
        chain: client.chain,
        providerId: options.providerId,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function isProviderActiveCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.serviceProviderRegistry.abi,
        address: options.contractAddress ?? chain.contracts.serviceProviderRegistry.address,
        functionName: 'isProviderActive',
        args: [options.providerId],
    };
}
//# sourceMappingURL=is-provider-active.js.map