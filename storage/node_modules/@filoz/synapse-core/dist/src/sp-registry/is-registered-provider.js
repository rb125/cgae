import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function isRegisteredProvider(client, options) {
    const data = await readContract(client, isRegisteredProviderCall({
        chain: client.chain,
        provider: options.provider,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function isRegisteredProviderCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.serviceProviderRegistry.abi,
        address: options.contractAddress ?? chain.contracts.serviceProviderRegistry.address,
        functionName: 'isRegisteredProvider',
        args: [options.provider],
    };
}
//# sourceMappingURL=is-registered-provider.js.map