import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getProviderIdByAddress(client, options) {
    const data = await readContract(client, getProviderIdByAddressCall({
        chain: client.chain,
        providerAddress: options.providerAddress,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function getProviderIdByAddressCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.serviceProviderRegistry.abi,
        address: options.contractAddress ?? chain.contracts.serviceProviderRegistry.address,
        functionName: 'getProviderIdByAddress',
        args: [options.providerAddress],
    };
}
//# sourceMappingURL=get-provider-id-by-address.js.map