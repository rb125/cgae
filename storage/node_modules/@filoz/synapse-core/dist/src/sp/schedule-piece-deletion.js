import { signSchedulePieceRemovals } from "../typed-data/sign-schedule-piece-removals.js";
import * as SP from "./sp.js";
export async function schedulePieceDeletion(client, options) {
    return SP.deletePiece({
        serviceURL: options.serviceURL,
        dataSetId: options.dataSetId,
        pieceId: options.pieceId,
        extraData: await signSchedulePieceRemovals(client, {
            clientDataSetId: options.clientDataSetId,
            pieceIds: [options.pieceId],
        }),
    });
}
//# sourceMappingURL=schedule-piece-deletion.js.map