import type { Account, Chain, Client, Transport } from 'viem';
import { DataSetNotFoundError } from '../errors/warm-storage.ts';
import * as Piece from '../piece.ts';
import { signAddPieces } from '../typed-data/sign-add-pieces.ts';
import type { PdpDataSet } from '../warm-storage/types.ts';
import * as SP from './sp.ts';
export declare namespace upload {
    type Events = {
        pieceUploaded: {
            pieceCid: Piece.PieceCID;
            dataSet: PdpDataSet;
        };
        pieceParked: {
            pieceCid: Piece.PieceCID;
            url: string;
            dataSet: PdpDataSet;
        };
    };
    type OptionsType = {
        dataSetId: bigint;
        data: File[];
        onEvent?: <T extends keyof upload.Events>(event: T, data: upload.Events[T]) => void;
    };
    type OutputType = {
        pieceCid: Piece.PieceCID;
        url: string;
        metadata: {
            name: string;
            type: string;
        };
    };
    type ErrorType = DataSetNotFoundError | SP.uploadPiece.ErrorType | SP.findPiece.ErrorType | SP.addPieces.ErrorType | signAddPieces.ErrorType;
}
export declare function upload(client: Client<Transport, Chain, Account>, options: upload.OptionsType): Promise<{
    pieces: {
        pieceCid: import("@web3-storage/data-segment").PieceLink;
        url: string;
        metadata: {
            name: string;
            type: string;
        };
    }[];
    txHash: import("viem").Hex;
    statusUrl: string;
}>;
//# sourceMappingURL=upload.d.ts.map