import { decodeFunctionData, encodeAbiParameters } from 'viem';
import * as Abis from "../../abis/index.js";
export function erc20CallHandler(data, options) {
    let functionName;
    let args;
    try {
        const decoded = decodeFunctionData({
            abi: Abis.erc20WithPermit,
            data: data,
        });
        functionName = decoded.functionName;
        args = decoded.args;
    }
    catch {
        throw new Error(`ERC20: failed to decode function data: ${data}`);
    }
    if (options.debug) {
        console.debug('ERC20: calling function', functionName, 'with args', args);
    }
    switch (functionName) {
        case 'balanceOf': {
            if (!options.erc20?.balanceOf) {
                throw new Error('ERC20: balanceOf is not defined');
            }
            return encodeAbiParameters(Abis.erc20WithPermit.find((abi) => abi.type === 'function' && abi.name === 'balanceOf').outputs, options.erc20.balanceOf(args));
        }
        case 'symbol': {
            if (!options.erc20?.symbol) {
                throw new Error('ERC20: symbol is not defined');
            }
            return encodeAbiParameters(Abis.erc20WithPermit.find((abi) => abi.type === 'function' && abi.name === 'symbol').outputs, options.erc20.symbol(args));
        }
        case 'decimals': {
            if (!options.erc20?.decimals) {
                throw new Error('ERC20: decimals is not defined');
            }
            return encodeAbiParameters(Abis.erc20WithPermit.find((abi) => abi.type === 'function' && abi.name === 'decimals').outputs, options.erc20.decimals(args));
        }
        case 'allowance': {
            if (!options.erc20?.allowance) {
                throw new Error('ERC20: allowance is not defined');
            }
            return encodeAbiParameters(Abis.erc20WithPermit.find((abi) => abi.type === 'function' && abi.name === 'allowance').outputs, options.erc20.allowance(args));
        }
        case 'name': {
            if (!options.erc20?.name) {
                throw new Error('ERC20: name is not defined');
            }
            return encodeAbiParameters(Abis.erc20WithPermit.find((abi) => abi.type === 'function' && abi.name === 'name').outputs, options.erc20.name(args));
        }
        case 'approve': {
            if (!options.erc20?.approve) {
                throw new Error('ERC20: approve is not defined');
            }
            return encodeAbiParameters(Abis.erc20WithPermit.find((abi) => abi.type === 'function' && abi.name === 'approve').outputs, options.erc20.approve(args));
        }
        case 'version': {
            if (!options.erc20?.version) {
                throw new Error('ERC20: version is not defined');
            }
            return encodeAbiParameters(Abis.erc20WithPermit.find((abi) => abi.type === 'function' && abi.name === 'version').outputs, options.erc20.version(args));
        }
        case 'nonces': {
            if (!options.erc20?.nonces) {
                throw new Error('ERC20: nonces is not defined');
            }
            return encodeAbiParameters(Abis.erc20WithPermit.find((abi) => abi.type === 'function' && abi.name === 'nonces').outputs, options.erc20.nonces(args));
        }
        default: {
            throw new Error(`ERC20: unknown function: ${functionName} with args: ${args}`);
        }
    }
}
//# sourceMappingURL=erc20.js.map