import { decodeFunctionData, encodeAbiParameters } from 'viem';
import * as Abis from "../../abis/index.js";
export function paymentsCallHandler(data, options) {
    const { functionName, args } = decodeFunctionData({
        abi: Abis.filecoinPay,
        data: data,
    });
    if (options.debug) {
        console.debug('Payments: calling function', functionName, 'with args', args);
    }
    switch (functionName) {
        case 'operatorApprovals': {
            if (!options.payments?.operatorApprovals) {
                throw new Error('Payments: operatorApprovals is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'operatorApprovals').outputs, options.payments.operatorApprovals(args));
        }
        case 'setOperatorApproval': {
            if (!options.payments?.setOperatorApproval) {
                throw new Error('Payments: setOperatorApproval is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'setOperatorApproval').outputs, options.payments.setOperatorApproval(args));
        }
        case 'accounts': {
            if (!options.payments?.accounts) {
                throw new Error('Payments: accounts is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'accounts').outputs, options.payments.accounts(args));
        }
        case 'deposit': {
            if (!options.payments?.deposit) {
                throw new Error('Payments: deposit is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'deposit').outputs, options.payments.deposit(args));
        }
        case 'depositWithPermit': {
            if (!options.payments?.depositWithPermit) {
                throw new Error('Payments: depositWithPermit is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'depositWithPermit').outputs, options.payments.depositWithPermit(args));
        }
        case 'depositWithPermitAndApproveOperator': {
            if (!options.payments?.depositWithPermitAndApproveOperator) {
                throw new Error('Payments: depositWithPermitAndApproveOperator is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'depositWithPermitAndApproveOperator')
                .outputs, options.payments.depositWithPermitAndApproveOperator(args));
        }
        case 'getRail': {
            if (!options.payments?.getRail) {
                throw new Error('Payments: getRail is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'getRail').outputs, options.payments.getRail(args));
        }
        case 'getRailsForPayerAndToken': {
            if (!options.payments?.getRailsForPayerAndToken) {
                throw new Error('Payments: getRailsForPayerAndToken is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'getRailsForPayerAndToken').outputs, options.payments.getRailsForPayerAndToken(args));
        }
        case 'getRailsForPayeeAndToken': {
            if (!options.payments?.getRailsForPayeeAndToken) {
                throw new Error('Payments: getRailsForPayeeAndToken is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'getRailsForPayeeAndToken').outputs, options.payments.getRailsForPayeeAndToken(args));
        }
        case 'settleRail': {
            if (!options.payments?.settleRail) {
                throw new Error('Payments: settleRail is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'settleRail').outputs, options.payments.settleRail(args));
        }
        case 'settleTerminatedRailWithoutValidation': {
            if (!options.payments?.settleTerminatedRailWithoutValidation) {
                throw new Error('Payments: settleTerminatedRailWithoutValidation is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'settleTerminatedRailWithoutValidation')
                .outputs, options.payments.settleTerminatedRailWithoutValidation(args));
        }
        case 'withdraw': {
            if (!options.payments?.withdraw) {
                throw new Error('Payments: withdraw is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'withdraw').outputs, options.payments.withdraw(args));
        }
        case 'withdrawTo': {
            if (!options.payments?.withdrawTo) {
                throw new Error('Payments: withdrawTo is not defined');
            }
            return encodeAbiParameters(Abis.filecoinPay.find((abi) => abi.type === 'function' && abi.name === 'withdrawTo').outputs, options.payments.withdrawTo(args));
        }
        default: {
            throw new Error(`Payments: unknown function: ${functionName} with args: ${args}`);
        }
    }
}
//# sourceMappingURL=payments.js.map