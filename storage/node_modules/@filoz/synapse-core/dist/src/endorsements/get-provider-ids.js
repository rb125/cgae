import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getProviderIds(client, options = {}) {
    const data = await readContract(client, getProviderIdsCall({
        chain: client.chain,
        contractAddress: options.contractAddress,
    }));
    return parseGetProviderIds(data);
}
export function getProviderIdsCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.endorsements.abi,
        address: options.contractAddress ?? chain.contracts.endorsements.address,
        functionName: 'getProviderIds',
        args: [],
    };
}
export function parseGetProviderIds(data) {
    return new Set(data);
}
//# sourceMappingURL=get-provider-ids.js.map