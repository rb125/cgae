import { encodeAbiParameters, } from 'viem';
import { randU256 } from "../utils/rand.js";
import { signAddPieces } from "./sign-add-pieces.js";
import { signCreateDataSet } from "./sign-create-dataset.js";
export const signcreateDataSetAndAddPiecesAbiParameters = [{ type: 'bytes' }, { type: 'bytes' }];
export async function signCreateDataSetAndAddPieces(client, options) {
    const clientDataSetId = options.clientDataSetId ?? randU256();
    const dataSetExtraData = await signCreateDataSet(client, { ...options, clientDataSetId });
    const addPiecesExtraData = await signAddPieces(client, { ...options, clientDataSetId });
    return encodeAbiParameters(signcreateDataSetAndAddPiecesAbiParameters, [dataSetExtraData, addPiecesExtraData]);
}
//# sourceMappingURL=sign-create-dataset-add-pieces.js.map