import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getApprovedProviders(client, options = {}) {
    const data = await readContract(client, getApprovedProvidersCall({
        chain: client.chain,
        offset: options.offset,
        limit: options.limit,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function getApprovedProvidersCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.fwssView.abi,
        address: options.contractAddress ?? chain.contracts.fwssView.address,
        functionName: 'getApprovedProviders',
        args: [options.offset ?? 0n, options.limit ?? 0n],
    };
}
//# sourceMappingURL=get-approved-providers.js.map