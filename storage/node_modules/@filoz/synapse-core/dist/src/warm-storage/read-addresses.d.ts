import type { Address, Chain, Client, MulticallErrorType, Transport } from 'viem';
import { asChain } from '../chains.ts';
export declare namespace readAddresses {
    type OptionsType = {
        contractAddress?: Address;
    };
    type OutputType = {
        payments: Address;
        warmStorageView: Address;
        pdpVerifier: Address;
        serviceProviderRegistry: Address;
        sessionKeyRegistry: Address;
        usdfcToken: Address;
        filBeamBeneficiary: Address;
    };
    type ErrorType = asChain.ErrorType | MulticallErrorType;
}
export declare function readAddresses(client: Client<Transport, Chain>, options?: readAddresses.OptionsType): Promise<readAddresses.OutputType>;
//# sourceMappingURL=read-addresses.d.ts.map