import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getClientDataSets(client, options) {
    const data = await readContract(client, getClientDataSetsCall({
        chain: client.chain,
        address: options.address,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function getClientDataSetsCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.fwssView.abi,
        address: options.contractAddress ?? chain.contracts.fwssView.address,
        functionName: 'getClientDataSets',
        args: [options.address],
    };
}
//# sourceMappingURL=get-client-data-sets.js.map