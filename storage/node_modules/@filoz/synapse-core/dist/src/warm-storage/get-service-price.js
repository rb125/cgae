import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getServicePrice(client, options = {}) {
    const data = await readContract(client, getServicePriceCall({
        chain: client.chain,
        contractAddress: options.contractAddress,
    }));
    return data;
}
export function getServicePriceCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.fwss.abi,
        address: options.contractAddress ?? chain.contracts.fwss.address,
        functionName: 'getServicePrice',
        args: [],
    };
}
//# sourceMappingURL=get-service-price.js.map