import { readContract } from 'viem/actions';
import { asChain } from "../chains.js";
export async function getDataSet(client, options) {
    const data = await readContract(client, getDataSetCall({
        chain: client.chain,
        dataSetId: options.dataSetId,
        contractAddress: options.contractAddress,
    }));
    if (data.pdpRailId === 0n) {
        return undefined;
    }
    return data;
}
export function getDataSetCall(options) {
    const chain = asChain(options.chain);
    return {
        abi: chain.contracts.fwssView.abi,
        address: options.contractAddress ?? chain.contracts.fwssView.address,
        functionName: 'getDataSet',
        args: [options.dataSetId],
    };
}
//# sourceMappingURL=get-data-set.js.map