{
  "name": "cgae-filecoin-storage",
  "version": "1.0.0",
  "description": "Synapse SDK uploader for CGAE audit certificates",
  "type": "module",
  "private": true,
  "scripts": {
    "upload": "node upload_to_synapse.mjs",
    "check": "node -e \"import('@filoz/synapse-sdk').then(m => console.log('SDK ok:', Object.keys(m))).catch(e => console.error('SDK missing:', e.message))\""
  },
  "dependencies": {
    "@filoz/synapse-sdk": "^0.37.0",
    "viem": "^2.46.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
